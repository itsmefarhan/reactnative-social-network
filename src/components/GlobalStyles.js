import { StyleSheet } from "react-native";

const globalStyles = StyleSheet.create({
  container: {
    flexDirection: "row",
    marginHorizontal: 20,
    marginVertical: 10
  },
  image: {
    width: 50,
    height: 50,
    borderRadius: 30
  }
});

export default globalStyles;
